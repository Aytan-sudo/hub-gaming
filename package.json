{
  "name": "hub-gaming",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/verifier.mjs",
    "serve": "python3 -m http.server 8780 --directory ..",
    "distribuer": "node scripts/distribuer.mjs",
    "verifier:copies": "node scripts/distribuer.mjs --check"
  }
}
